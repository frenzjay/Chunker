# Chunker Web Server Configuration

# Server port (default: 3001)
PORT=3001

# Path to Chunker CLI jar file (auto-detected if not specified)
# CLI_PATH=/path/to/chunker-cli.jar

# Java options for the CLI backend (e.g., "-Xmx4G -Xms1G")
JAVA_OPTIONS=

# Maximum file upload size in bytes (default: 5GB)
# MAX_FILE_SIZE=5368709120

# Node environment (development or production)
NODE_ENV=production
